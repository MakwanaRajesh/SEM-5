@model List<Reastaurant.Models.Category>
@{
    ViewData["Title"] = "Menu";
}
@{
    Layout = "_Layout"; // ✅ Moved to the top

    ViewData["Title"] = "Dashboard";
}

@* <main>

    <div class="container py-5">
        <div class="text-center mb-5">
            <h1 class="fw-bold">Browse by Category</h1>
            <p class="text-muted">Find your favorite type of food</p>
        </div>

        <div class="row">
            @foreach (var category in Model.Where(c => c.IsActive))
            {
                <div class="col-md-4 col-sm-6 mb-4">
                    <a href="/Menu/Category/@category.CategoryId" class="card text-white text-decoration-none bg-dark h-100">
                        <img src="@category.ImageUrl" class="card-img-top" style="height: 200px; object-fit: cover;" />
                        <div class="card-body">
                            <h5 class="card-title">@category.CategoryName</h5>
                            <p class="card-text small">@category.Description</p>
                        </div>
                    </a>
                </div>
            }
        </div>
    </div>
</main> *@